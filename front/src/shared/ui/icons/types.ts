export interface IconProps {
  /** Цвет в любом валидном формате RN (rgb, hex, named). По умолчанию — currentColor родителя. */
  color?: string;
  /** Размер квадрата иконки в логических пикселях. */
  size?: number;
}
