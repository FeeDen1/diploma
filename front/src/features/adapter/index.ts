// StudentsList, SubmissionsList, StudentSubmissionsList перенесены в widgets/.
// Здесь остаётся только GroupsList — простой компактный список групп без
// сложной композиции, валидно для feature-слоя.
export { GroupsList } from './ui/GroupsList';
