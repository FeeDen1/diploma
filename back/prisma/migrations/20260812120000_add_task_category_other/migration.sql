-- Добавляем категорию «Разное» (other) в набор категорий заданий.
--
-- Postgres 12+ позволяет ADD VALUE внутри транзакции (Prisma applies миграции
-- в транзакции) — при условии, что новое значение не используется в той же
-- транзакции. Здесь мы только добавляем его, поэтому безопасно.
ALTER TYPE "TaskCategory" ADD VALUE IF NOT EXISTS 'other';
